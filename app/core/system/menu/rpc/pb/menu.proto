syntax = "proto3";

package core.system.menu.rpc.v1;
option go_package="./pb";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Service {
  rpc Ping(Request) returns(Response);
}
